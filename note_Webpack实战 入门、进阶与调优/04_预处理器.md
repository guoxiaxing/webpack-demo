默认 webpack 只能对 js 进行打包，为了使 webpack 可以处理图片，css，html 等其他资源，就有了预处理器 loader。

#### 一切皆模块

对于 webpack 来说一切静态资源，图片，html，css，js，字体都是一个模块，可以想加载一个 js 一样去加载这些静态资源。

```
// main.js
import './index.js';
import './style.css';
```

这句引用可以使 webpack 在打包的时候将 style.css 一起打包进来并输出到资源目录下；实际意义就是描述了 index.js 和 style.css 之间的依赖关系

#### loader

- 一个函数

- 公式 loader

```
output = loader(input)
```

input 可能是工程源文件的字符串，也可能是上一个 loader 的转换结果（也是字符串），source map，AST 对象。

output 也可能是转换后的文件字符串，source map，AST 对象；如果这是最后一个 loader 则会直接吧结果传递给 webpack 进行处理，否则就作为下一个 loader 的输入

loader 是链式的，我们可以为同一种类型的资源设置多个 loader，第一个 loader 的输入就是文件的源码，后面每一个 loader 的输入是上一个 loader 的输出

```
module.exports = function loader (content, map, meta) {
  var callback = this.async();
  var result = handler(content, map, meta);
  callback(
    null,           // error
    result.content, // 转换后的内容
    result.map,     // 转换后的 source-map
    result.meta,    // 转换后的 AST
  );
};
```

##### loader 的配置

loader 对 webpack 无法直接处理的资源进行转译，其实就是一个预处理的工作

```
 // 与loader相关的配置都在 module.rules 中
  module: {
    rules: [
      {
        // test 接受的是一个正则表达式，只有被正则表达式匹配上的文件才会应用这些loader
        test: /\.css$/,
        // use 接受一个数组，表示为这个文件所使用的所有loader
        // css-loader 仅仅处理的是 css 中的加载语法 @import 以及 url() 函数等
        // 如果想让样式起作用，还需要使用style-loader在页面中插入样式
        use: ["style-loader", "css-loader"]
      }
    ]
  },
```

**css-loader 通常和 style-loader 一起使用**

##### 链式 loader

对于 scss 文件来说：

1. 需要 sass-loader 来将 scss 语法转换为 css

2. 需要 css-loader 处理 css 的各类加载语法

3. 最后使用 style-loader 将样式字符串包装成 style 标签插入页面

webpack 打包的时候会将资源按照**从右往左**的顺序交给 loader 去处理

##### loader-options

```
  module: {
    rules: [
      {
        test: /\.css$/,
        use: [
          "style-loader",
          {
            loader: "css-loader",
            options: {
              // 配置项
            }
          }
        ]
      }
    ]
  },
```

##### 其他配置

1. exclude

```
  module: {
    rules: [
      {
        test: /\.css$/,
        use: [
          "style-loader",
          "css-loader",
        ],
        exclude: /node_modules/
      }
    ]
  },
```

2. include

```
rules: [
{
  test: /\.css$/,
    use: ['style-loader', 'css-loader'],
    include: /src/,
}
],
```

> include 和 exclude 同时存在时 exclude 的优先级更高

由于 exclude 优先级更高，我们可以对 include 中的子目录进行排除

```
rules: [
{
  test: /\.css$/,
    use: ['style-loader', 'css-loader'],
    exclude: /src\/lib/,
    include: /src/,
}
],
```

3. enforce

指定一个 loader 的种类 只接受 'pre'/'post'两个值

webpack 中 loader 的类型可以分为 pre/post/normal/inline 四种类型

直接定义的 loader 属于 normal 类型

inline 类型官方已不推荐使用

```
rules: [
  {
    test: /\.js$/,
    enforce: 'pre',
    use: 'eslint-loader',
  }
],
```

`eslint-loader`将在所有 normal loader 之前执行

类似的，如果某一个 loader 是需要在所有 loader 之后执行的，我们也可以指定其 enforce 为“post”

使用 enforce 可以强制保证 loader 的执行顺序

##### 常用 loader 介绍

1. babel-loader 将 es6+ 转换为 es5

```
npm install babel-loader @babel/core @babel/preset-env
```

- babel-core 使 babel 和 webpack 可以协同工作

- @babel/core babel 编译器的核心模块

- @babel-preset-env babel 的预置器，可以根据用户的目标环境自动添加所需的插件和补丁来对 es6 进行编译

```
rules:[{
        test: /\.js$/,
        use: [
          {
            loader: "babel-loader",
            // loader: "babel-loader?cacheDirectory=true"
            options: {
             cacheDirectory: true,
             // presets: ["@babel/preset-env"]
            presets: [
                [
                  "@babel/preset-env",
                  {
                    modules: false
                  }
                ]
              ]
            }
          }
        ],
        exclude:'node_modules'
      }]
```

**由于@babel/preset-env 会将 ES6 Module 转化为 CommonJS 的形式，这会导致 Webpack 中的 tree-shaking 特性失效。将@babel/preset-env 的 modules 配置项设置为 false 会禁用模块语句的转化，而将 ES6 Module 的语法交给 Webpack 本身处理。**

可以直接使用 .babelrc 文件来对 babel-loader 进行配置

- ts-loader

用于链接 webpack 和 typescript

```
npm install ts-loader typescript
```

Typescript 本身的配置并不在 ts-loader 中，而是必须要放在工程目录下的 tsconfig.json 中

```
{
        test: /\.ts$/,
        use: "ts-loader"
      }
```

- html-loader

html-loader 用于将 html 文件转换为字符串并格式化，使得我们可以把一个 html 自字符串通过 js 加载过来

```
npm install html-loader
```

```
{
        test: /\.html$/,
        use: "html-loader"
      }


```

- file-loader 用于打包文件类型的资源，并返回其 publicPath

**无论怎么配置 publicPath 都不会影响最后打包后的图片的输出目录（输出到 output.path 这个目录下）**

```
npm install file-loader
```

```
      { test: /\.(jpg|png|gif)$/, use: "file-loader" }
```

index.js

```
import testPng from "../assets/imgs/test.png";
console.log(testPng) // file:///Users/guoxx03/Desktop/test/webpack-demo/note_Webpack%E5%AE%9E%E6%88%98%20%E5%85%A5%E9%97%A8%E3%80%81%E8%BF%9B%E9%98%B6%E4%B8%8E%E8%B0%83%E4%BC%98/example/Chapter04/dist/072874e47fe026fd4229233b807ea477.png
```

```
// 配置了 output.publicPath './assets/' 之后图片的输出路径为
./assets/072874e47fe026fd4229233b807ea477.png
```

```
// 配置了 file-loader 的publicPath  './images/' 之后图片的输出路径为
./images/test.png
```

- url-loader

一般和 file-loader 一起配合使用，支持配置一个阈值（limit）大小大于这个阈值的文件使用 file-loader 处理返回文件相对于 publicPath 的路径，小于该阈值的文件使用 url-loader 处理，返回文件的 base64 编码

```
npm i url-loader
```

```
{
        test: /\.(jpg|png|gif)$/,
        loader: "url-loader",
        options: {
          limit: 10240000,
          name: "[name].[ext]",
          // 这个publicPath会覆盖output中的
          publicPath: "./images/"
        }
      }
```

index.js

```

import testPng from "../assets/imgs/test.png";
console.log(testPng)
// data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAlAAAAJQCAIAAADOgM0SAAAAAXNSR0IB2cksfwAAAAlwSFlzAAAuIwAALiMBeKU...

```

##### 自定义 loader

1. 基本实现

实现一个 loader 会为所有的 js 文件开启严格模式，即会在 js 文件的首部加上 'use strict'

根目录下新增一个 loaders 目录

force-strict-loader.js

```
module.exports = function(source) {
  const prefix = "'use strict'; \n\n";
  return prefix + source;
};
```

使用

```
{
        test: /\.js$/,
        use: path.resolve(__dirname, "loaders", "force-strict-loader"),
        exclude: /node_modules/
      }
```

2. 使用缓存

当文件及其依赖没有发生变化的时候，让 loader 直接使用缓存，而不是每次都进行重复的转换

force-strict-loader.js

```
module.exports = function(source) {
  // 使用缓存
  if (this.cacheable) {
    this.cacheable();
  }
  const prefix = "'use strict'; \n\n";
  return prefix + source;
};
```

3. 获取 options

依赖 loader-utils 这个工具库

```
const loaderUtils = require("loader-utils");

module.exports = function(source) {
  // 使用缓存
  if (this.cacheable) {
    this.cacheable();
  }
  // 获取和打印options
  const options = loaderUtils.getOptions(this) || {};
  console.log(options); // { sourceMap: true }

  const prefix = "'use strict'; \n\n";
  return prefix + source;
};
```

webpack.config.js

```
{
        test: /\.js$/,
        loader: path.resolve(__dirname, "loaders", "force-strict-loader"),
        options: {
          sourceMap: true
        },
        exclude: /node_modules/
      }
```

4. source map

可以让开发这在控制台上看到源码 需要安装 source-map 这个库

```
npm i source-map
```

```
const loaderUtils = require("loader-utils");
const SourceNode = require("source-map").SourceNode;
const SourceMapConsumer = require("source-map").SourceMapConsumer;

module.exports = function(source, sourceMap) {
  // 从loader中我们获取到我们的sourceMap对象，它是由上一个loader传下来的
  const prefix = "'use strict'; \n\n";

  // 使用缓存
  if (this.cacheable) {
    this.cacheable();
  }
  // 获取和打印options
  const options = loaderUtils.getOptions(this) || {};
  console.log(options); // { sourceMap: true }

  if (options.sourceMap && sourceMap) {
    const currentRequest = loaderUtils.getCurrentRequest(this);
    const node = SourceNode.fromStringWithSourceMap(
      source,
      new SourceMapConsumer(sourceMap)
    );
    node.prepend(prefix);
    const result = node.toStringWithSourceMap({ file: currentRequest });
    const callback = this.async();
    // 使用callback主要是为了一次性可以返回多个值
    // 三个参数分别是 发生的错误，处理过后的源码，以及source-map
    callback(null, result.code, result.map.toJSON());
  }
  // 不支持 sourceMap 的情况
  return prefix + source;
};
```
